events {
}
http {
    
    upstream frontend {
        ip_hash;
        server frontend:3000;
    }
    # upstream backend {
    #     ip_hash;
    #     server backend:8080;
    #     server 127.0.0.11 backup;
    # }
    server {
        location /{
            proxy_pass http://frontend;
        }
        # location /api {
        #     proxy_pass http://backend;
        # }
    }
}